{
  "id": "spinner",
  "title": "Spinner",
  "category": "feedback",
  "tier": "free",
  "structure": [
    "spinner-root",
    "spinner-trigger",
    "spinner-message",
    "spinner-severity",
    "spinner-dismiss"
  ],
  "requiredStates": [
    "default",
    "visible",
    "leaving",
    "success",
    "error",
    "warning"
  ],
  "codeExample": "```tsx\nexport function SpinnerExample() {\n  // Minimal Spinner pattern\n  return (\n    <section aria-label=\"Spinner\" data-pattern=\"spinner\">\n      <header>\n        <h2>Spinner</h2>\n      </header>\n      <div className=\"pattern-body\">\n        {/* Implement required states: default + interactive variants */}\n      </div>\n    </section>\n  );\n}\n```",
  "mistakes": [
    "Toast as the only record of a destructive action",
    "Modal without focus trap",
    "Spinner with no timeout or cancel",
    "Treating Spinner as decoration without visible state"
  ],
  "relatedPatterns": [
    "empty-state",
    "modal",
    "drawer"
  ]
}
