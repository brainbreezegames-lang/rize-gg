{
  "id": "age-gate",
  "title": "Age Gate",
  "category": "auth",
  "tier": "pro",
  "structure": [
    "age-gate-root",
    "age-gate-identity-input",
    "age-gate-verify",
    "age-gate-error",
    "age-gate-success"
  ],
  "requiredStates": [
    "default",
    "pending",
    "success",
    "error",
    "locked",
    "expired"
  ],
  "codeExample": "```tsx\nexport function AgeGateExample() {\n  // Minimal Age Gate pattern\n  return (\n    <section aria-label=\"Age Gate\" data-pattern=\"age-gate\">\n      <header>\n        <h2>Age Gate</h2>\n      </header>\n      <div className=\"pattern-body\">\n        {/* Implement required states: default + interactive variants */}\n      </div>\n    </section>\n  );\n}\n```",
  "mistakes": [
    "No rate-limit messaging on OTP",
    "Password rules shown only after failure",
    "Losing deep-link context after verify",
    "Treating Age Gate as decoration without pending state"
  ],
  "relatedPatterns": [
    "otp-input",
    "password-field",
    "kyc-upload"
  ]
}
