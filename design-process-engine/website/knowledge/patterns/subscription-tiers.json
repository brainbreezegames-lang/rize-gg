{
  "id": "subscription-tiers",
  "title": "Subscription Tiers",
  "category": "commerce",
  "tier": "pro",
  "structure": [
    "subscription-tiers-root",
    "subscription-tiers-offer-summary",
    "subscription-tiers-price",
    "subscription-tiers-cta",
    "subscription-tiers-trust-signals"
  ],
  "requiredStates": [
    "default",
    "selected",
    "loading",
    "sold-out",
    "discounted",
    "error"
  ],
  "codeExample": "```tsx\nexport function SubscriptionTiersExample() {\n  // Minimal Subscription Tiers pattern\n  return (\n    <section aria-label=\"Subscription Tiers\" data-pattern=\"subscription-tiers\">\n      <header>\n        <h2>Subscription Tiers</h2>\n      </header>\n      <div className=\"pattern-body\">\n        {/* Implement required states: default + interactive variants */}\n      </div>\n    </section>\n  );\n}\n```",
  "mistakes": [
    "Price without currency or tax clarity",
    "CTA that does not restate the commitment",
    "Hiding fees until the last step",
    "Treating Subscription Tiers as decoration without selected state"
  ],
  "relatedPatterns": [
    "paywall",
    "pricing-table",
    "checkout-summary"
  ]
}
