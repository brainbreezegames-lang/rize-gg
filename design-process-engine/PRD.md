# PRD — Design Process Engine for AI Agents

Working name TBD · Version 1.0 · August 2026

## Shipped in this package

**Product B — Engine:** stateful MCP (`start_task`, `get_playbook`, `submit_plan`, `get_pattern_guide`, `review`, `final_check`) + companion skill + free/pro tier gating + public website.

**Product A — Factory:** capture → stack → draft → human curate pipeline with fixtures and CLI.

**Knowledge:** 10 flow playbooks, 100 pattern guides, monthly slop catalog (regenerable via `npm run knowledge:generate`).

## Thesis

Everyone else ships knowledge; we ship **enforcement**, with knowledge injected at the right moments.

## Deferred (intentionally)

Mobile-app capture at scale, team plans / shared brand contracts, private playbooks, game-development edition, Stripe billing UI (keys are env-based for now).
