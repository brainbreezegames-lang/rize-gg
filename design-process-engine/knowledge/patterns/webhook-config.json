{
  "id": "webhook-config",
  "title": "Webhook Config",
  "category": "settings",
  "tier": "pro",
  "structure": [
    "webhook-config-root",
    "webhook-config-section-nav",
    "webhook-config-group",
    "webhook-config-control",
    "webhook-config-save"
  ],
  "requiredStates": [
    "default",
    "dirty",
    "saving",
    "saved",
    "error",
    "read-only"
  ],
  "codeExample": "```tsx\nexport function WebhookConfigExample() {\n  // Minimal Webhook Config pattern\n  return (\n    <section aria-label=\"Webhook Config\" data-pattern=\"webhook-config\">\n      <header>\n        <h2>Webhook Config</h2>\n      </header>\n      <div className=\"pattern-body\">\n        {/* Implement required states: default + interactive variants */}\n      </div>\n    </section>\n  );\n}\n```",
  "mistakes": [
    "Autosave with no confirmation of what changed",
    "Dangerous actions adjacent to benign toggles",
    "Read-only SSO fields looking editable",
    "Treating Webhook Config as decoration without dirty state"
  ],
  "relatedPatterns": [
    "settings-page",
    "cookie-consent",
    "invite-team"
  ]
}
